function main() returns Int:
    foreach(i of 10) 
        if(i % 2 == 0)
            printInt(i)
    printc(10)
    
    foreach(i of 10) 
        if(i % 2 == 1):
            printInt(i)
            printc(10)
        end
	return 0
end
