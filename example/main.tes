imp "./2.tes"

function main() returns Int:
    val Array a;
    a = createArray(a);

    foreach(i of sumList(a)):
        if(i == 2):
            i = i + 1;
        end
        printInt(i);
    end
    
	return 0;
end
