function find(Array A,Int x) returns Int:
    val Int i;
    i = 0;

    foreach (a of 10):
        if (x == a):
            return i;
        end
            i = i + 1;
    end
    i = 1 ? i + 1 : i *5;
    return -1;
end

function main() returns Int:
    val Array A;
    val Int b;
    
    A = createArray(5);
    
    b = find(A,5);
    
    printInt(b);
    
    return 0;
end