function main() returns Int:
    val Array A;
    A = createArray(5);
    
    foreach(i of arrayLength(A)):
        A[i] = getInt();
    end

    foreach(a of A):
        printInt(a);
    end

	return 0;
end
