imp "./2.tes"

function setcb(Int b,Int c,Int d) returns Int:
    b = 10;
    c = 5;
end

function main() returns Int:
    val Int c;
    val Int b;
    val Array a;
    
    setcb(20,c,b);

    a = createArray(a);

    foreach(i of sumList(a)):
        if(i == 2):
            i = i + 1;
        end
        printInt(i);
    end
    
	return 0;
end
