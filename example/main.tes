imp "./shyn48.tes"

function find(Array A,Int x) 
returns Int:
    val Int n;
    val Int b;
    i = 0;
    foreach (a of A)
        if (n == k):
            return i;
        end
            i = i + 1;
    end
    return -1;
end
function man() returns Int:
    val Array A;
    val Array B;
    val Int a;
    a = sum(A,a,a);
    a= a + 10;
    find(A, a)
    find(B)
    printInt(find(a, A));
    B = a > 10 ? a : A;
    A = createArray(3,0);
    return A;
end